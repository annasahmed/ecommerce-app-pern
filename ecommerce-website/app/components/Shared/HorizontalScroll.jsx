import React from "react";

const HorizontalScroll = ({ children }) => {
	return (
		<section className="flex flex-nowrap gap-6 container-layout-left-only overflow-x-auto hide-scrollbar">
			HorizontalScroll
		</section>
	);
};

export default HorizontalScroll;
